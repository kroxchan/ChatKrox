# 2026-02-21 日志（近24h复盘明细）

## 会话盘点与产出

### 1) 系统维护：Windows PATH 修复 + openclaw 可执行验证
- 场景：用户侧出现 openclaw 找不到/不同终端 PATH 不一致。
- 动作：
  - 发现用户级 `HKCU\\Environment\\Path` 混入字面量 `%PATH%`，导致解析不稳定。
  - 通过 `reg add` 直接将用户级 Path 指向 `C:\\Users\\Krox\\AppData\\Roaming\\npm`，确保 `openclaw` 可被解析。
  - 用 `where openclaw`、`openclaw --version` 做现场验证（版本 `2026.2.19-2`，且为 npm 最新）。
- gotcha：
  - 过程中有一次 PowerShell 一行命令转义失败，出现 `= : 无法将=识别为 cmdlet`；属于命令拼接问题，不影响最终修复。
  - 将用户级 Path 直接“清空重建”为 npm 目录会影响用户原本依赖的 Python/Go/bun/cargo 等路径；需要后续按清单安全加回。
- 未完成：
  - “飞书插件 duplicate id”告警是否需要处理（取决于是否使用飞书插件）。
  - 用户如需恢复其它工具链路径，需要收集其常用工具清单后重建 Path。

### 2) 法律/行政维权文书：围绕“档案管理失范”与“欠薪线索平台”
- 对象：QQ 私聊 openid `E01D...A399`。
- 关键策略：
  - 将核心战场锁定为“档案管理问题”（法院审理中已调取/呈现的客观线索），用以倒逼工资社保问题进入实质解决。
  - 给出三套可投递文书草案：市纪委监委（证据封存+责任倒查）、市政府督办（提级/回避/限期）、省人社厅（行业监督+责令规范）。
  - 解释机关偏好：通常更倾向协商+程序性处置而非诉讼；但司法程序揭示“原始档案材料不间断出现/再次调取到双方均未掌握材料”会显著提升处理压力。
  - 国务院客户端“欠薪线索反映”定位为加速器/留痕督办工具：属地仍会转办，但更可追踪、更有时限约束。
- 产出：
  - 生成“国务院客户端欠薪线索”长版、500字版、300字版可直接粘贴文本。
  - 针对判决书关键原话做“证据要点提炼 + 文本C升级段落 + 预设反驳回应”（避免对方用“不能归咎于两被告”来挡）。
- gotcha：
  - 欠薪平台更适配“现时欠薪/要件清晰”场景；对跨度极长、涉及事业单位档案与待遇链条的复杂历史问题，可能仍被导回其他渠道；需表述切割成平台能处理的最小闭环（核查、调取台账、书面结论）。
- 未完成：
  - 用户未选择要“最终定稿升级”的版本（文本A/文本C/两者）；等待其回 1/2/3。
  - 用户未补齐协商型履职申请里的期限、送达地址、投递窗口等细节（可进一步定稿）。

### 3) AMap 技能资产整理（索引落盘）
- 背景：确认本地存在可用 AMap(高德) skill 资产与 SOP。
- 产出：写入 `memory/amap-skill-index.md`，记录：
  - repo 路径、SOP/Checklist、入口命令、环境变量 `AMAP_MAPS_API_KEY` 存在（不回显）、已跑通示例（地理编码/周边 POI）。
- gotcha：
  - 需要后续决定把 `amap-skill` 以何种方式“正式挂载到技能库目录”。
- 未完成：
  - skill 入口正式挂载方案未落地（等待目录约定）。

### 4) 位置美食推荐（长沙南站附近，要求用高德）
- 现状：工具侧未能直接拿到用户实时坐标、也未接到高德接口；只能要求用户提供“出入口/地铁口/位置分享”后再按高德逻辑筛选。
- gotcha：
  - 用户说“可以用高德”，但仍需明确可定位信息；否则无法给出“最近+营业中”的结果。
- 未完成：
  - 等待用户提供更精确位置（东/西广场、地铁口、酒店名或位置分享）与口味偏好。

### 5) 求职简历框架（研一，2027.06毕业，经济学，广深，央企/外企）
- 产出：给出求职方向建议（经营分析/战略研究/产业研究/商业分析等）+ 一页简历模板（央企版/外企版可扩展）。
- 未完成：
  - 等待用户补齐工具栈熟练度、实习起止时间、可量化成果数字，以便生成最终定稿版简历。

### 6) 家人健康与问候类定时任务（天气+金句）
- 晚安/午安：对主人/老婆/家家等生成中文问候，天气优先 Open-Meteo；包含古文/白话/呼吸或睡前建议等。
- 家家：出现服药提醒、晚间测血压提醒等。
- gotcha：
  - “妈妈晚安”那条在 Open-Meteo可取数情况下误写为“天气数据暂不可用”，属于 weather fetch 结果解析/判断分支问题（需要改进：只在 fetch 失败或缺字段时降级）。

### 7) 小红书内容日历写入（中午提醒 & 晚间第二条）
- 中午提醒：曾因 `.openviking_data/.../51_Content_calendar_*.md` 文件名编码/路径解析问题导致无法稳定写入与更新状态（当时未完成）。
- 晚间第二条：后来通过写入工作区根 `XHS_OPS.md` 方式落盘，并将状态改为 `posted`；备注“需用户在小红书端手动发布”。
- gotcha：
  - 同一套内容日历存在“真实源文件在 .openviking_data”与“工作区根 XHS_OPS.md”两套来源，容易产生分叉；需明确单一事实源（source of truth）。
- 未完成：
  - 需要决定：以 `.openviking_data/...` 作为唯一源并修复编码写入，或以工作区根 `XHS_OPS.md` 为主并建立同步策略。
  - 若要做发布后 2h 数据记录，需创建/维护 `memory/xhs-progress.md` 并定时写入曝光/赞/藏/评。

## 复盘：可迁移经验（简短）
- 对复杂行政/维权问题：先把“硬事实线索”锁死（法院调取/新证据不断出现）再设计多通道（协商主线 + 平台留痕 + 升级督办）。
- 对本地工具/路径问题：优先做“可验证的最小修复”，但要明确副作用（清空 Path 其他条目）并列出回滚/重建方案。
- 对内容运营 SOP：必须先明确唯一源文件路径与编码，否则自动化写入会出现“看似写了但写到另一个文件”的隐患。
